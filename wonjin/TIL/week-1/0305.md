- 성능적으로 인라인 스타일링보다 스타일 객체를 만드는게 좋다 (드라마틱하진 않으니 상황에 맞게)
- react-native css cheat sheet를 통해 지원 안 하는 스타일링을 인지하자
- `SafeAreaView`
  > 아이폰의 노치 같은 컨텐츠가 짤리는 공간을 제외하여 View를 할당
- 조건에 따라 스타일링이 필요한 경우 (스타일 시트에는 조건을 넣을 수 없다)

```
<Text style={[1번스타일,{
  color:
    조건
      ? true스타일
      : false스타일
}]}
```

- Flipper를 통한 디버깅 시도 해보자

- 재실행시에 캐시 삭제에 유의
